import 'package:flutter/material.dart';

class FlashManagePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Center(
      child: new Text("闪光灯控制"),
    );
  }

}